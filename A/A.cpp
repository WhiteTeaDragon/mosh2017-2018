#define _CRT_SECURE_NO_WARNINGS
#include<iostream>
#include<fstream>
#include<vector>
#include<algorithm>
#include<string>
#include<stack>
#include<cmath>
#include<map>
#include<set>
#include<deque>
#include<climits>
#include<sstream>
//#pragma comment(linker, "/STACK:5000000000")

using namespace std;

int main()
{
	FILE* in = fopen("a0.txt", "r");
	FILE* out = fopen("a0ans.txt", "w");
	vector<string> ans(1000 * 1000);
	for (int i = 0; i < 1000; ++i)
	{
		for (int j = 0; j < 1000; ++j)
		{

		}
	}
	int n;
	fscanf(in, "%d", &n);
	for (int i = 0; i < n; ++i)
	{
		for (int j = 0; j < )
	}
	//system("pause");
	return 0;
}